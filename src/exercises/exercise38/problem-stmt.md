i have aws edge node which has lots of libraries already installed. We want install about 15 more python modules that qe
need to install. we do not wan to update the version of the libraries that are aleady install. 

Write code such that user performs pip list and get the lsit of currenly installed libraries, we do use pypi, we have our
own ecit, i can update the global.index url,  make use of this intalled library list and
new library list and index url