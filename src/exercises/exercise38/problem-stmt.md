i have aws edge node which has lots of libraries already installed. We want install about 15 more python modules that qe
need to install. we do not wan to update the version of the libraries that are aleady install. can we write a script to
figure out the right version of the new libraries that can be installed with the existing already install pythn
module/libraries